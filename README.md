# Telegram-бот для подбора конфигураций ПК

## Описание

Telegram-бот, который помогает пользователям подобрать оптимальную конфигурацию персонального компьютера по бюджету и целям. Бот учитывает:
- Бюджет пользователя.
- Цель сборки (игры, офис, монтаж и т.д.).
- Совместимость компонентов.
- Производительность (Cinebench, TDP, частоты и т.д.).

## Функционал

- Ввод **бюджета** пользователя.
- Выбор **цели сборки**:
  - Игры
  - Офис
  - Монтаж
- Подбор компонентов:
  - CPU (процессор)
  - GPU (видеокарта)
  - MB (материнская плата)
  - RAM (оперативная память)
  - SSD (накопитель)
  - PSU (блок питания)
  - Case (корпус)
  - Cooler (кулер)
- **Совместимость** компонентов (socket, TDP, форм-фактор и т.д.).
- **Расчёт общей стоимости** сборки.
- **Сохранение** сборок пользователя в базе данных.
- **Просмотр** предыдущих сборок.
- **Поиск магазинов** (на примере DNS) в выбранном городе с помощью **Яндекс.Карт API**.

## Используемые технологии

- **Python**
- **python-telegram-bot** — для взаимодействия с Telegram Bot API.
- **Docker** и **docker-compose** — для контейнеризации.
- **SQLite** — для хранения сборок пользователей.
- **pandas**, **numpy**, **matplotlib** — для анализа данных.
- **requests** — для взаимодействия с внешними API.
- **Yandex Maps API** — для геокодирования и статических карт.

## Структура проекта
pc-builder-bot/
├── bot/
│ ├── init.py
│ ├── bot.py
│ ├── handlers.py
│ ├── config.py
│ ├── database.py
│ ├── selector.py
│ └── data/
│ ├── cpu.json
│ ├── gpu.json
│ ├── ram.json
│ ├── ssd.json
│ ├── psu.json
│ ├── case.json
│ ├── coolers.json
│ └── motherboard.json
├── docker-compose.yml
├── Dockerfile
├── requirements.txt
├── .env
└── README.md

## Запуск проекта

### 1. Клонирование репозитория

```bash
git clone https://github.com/your-username/pc-builder-bot.git
cd pc-builder-bot
```

### 2. Установка Docker и Docker Compose
Убедитесь, что у вас установлены:

- **Docker**
- **Docker Compose**

### 3. Настройка переменных окружения
Создайте файл .env в корне проекта и укажите:

- **TELEGRAM_TOKEN=your_token_here**
- **YANDEX_MAPS_API_KEY=your_yandex_maps_api_key**

### 4. Запуск с помощью Docker Compose
```bash
docker-compose up --build
```

Бот запустится в контейнере и будет готов к работе.

## Использование
- Найдите вашего бота в Telegram.
- Отправьте команду /start.
- Выберите "Создать новую сборку".
- Укажите бюджет и цель сборки.
- Получите рекомендованную конфигурацию и её стоимость.
- (Опционально) Нажмите "Найти магазины", чтобы получить ссылку на Яндекс.Карты.

## Анализ данных
В проекте предусмотрен скрипт для анализа данных о комплектующих с использованием **pandas**, **numpy**, **matplotlib**. Это позволяет:

Построить графики зависимости цены от производительности.
Сравнить бренды (Intel vs AMD).
Провести статистический анализ.

## Актуальность проекта
Сложность подбора комплектующих.
Рост интереса к самостоятельной сборке ПК.
Помощь новичкам и экономия времени.
Персонализированные рекомендации.
Анализ производительности и цены.

## Лицензия
MIT